manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: caophuoclong
      url-base: https://github.com/caophuoclong
  projects:
    - name: zmk
      remote: caophuoclong
      # Fork of zmkfirmware/zmk, branch `v0.3-extra-effects`: the v0.3 tag plus
      # the chase/twinkle/fire/gradient/highlight underglow effects, which have
      # to live in the core since rgb_underglow.c keeps its pixel buffer and
      # effect list private. Rebase this branch on top of newer ZMK releases to
      # upgrade.
      revision: b5f1aecc4ea2e3daa6b00d60ba244130649b11f1
      import: app/west.yml
    - name: zmk-nice-oled
      remote: caophuoclong
      # Branch `persistent-layer`, based on 46f824a (before two regressions vs.
      # zmk v0.3: the LVGL v9 image-descriptor rename on main, and a5ec2f9
      # dropping the ${CMAKE_SOURCE_DIR}/include path needed for
      # zmk/endpoints.h). Adds CONFIG_NICE_OLED_PERSISTENT_LAYER.
      revision: e6211c5499699780a3735781212d716e29b0c599
  self:
    path: config
