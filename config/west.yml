manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: caophuoclong
      url-base: https://github.com/caophuoclong
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: v0.3
      import: app/west.yml
    - name: zmk-nice-oled
      remote: caophuoclong
      # Pinned before two regressions vs. zmk v0.3 (pinned above): the LVGL v9
      # image-descriptor rename (lv_img_dsc_t -> lv_image_dsc_t) on main, and
      # a5ec2f9 dropping the ${CMAKE_SOURCE_DIR}/include path from
      # CMakeLists.txt (needed to find zmk/endpoints.h).
      revision: 46f824abb2bd41f1287c5c68abd14122af6042a3
  self:
    path: config
